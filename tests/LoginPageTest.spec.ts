import {test, expect} from '@playwright/test'


test("Login into application", async ({page})=>{
    // Create and object for Loginpage
    // Call the different methods inside the test file
    // Assertion to check if the test passed or failed
})